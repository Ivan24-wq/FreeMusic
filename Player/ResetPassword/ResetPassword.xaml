<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Player.ResetPassword.ResetPassword">
    
    <ScrollView>
        <VerticalStackLayout>
            <!-- Поле Пароль -->
            <Frame BackgroundColor="White" CornerRadius="10" HasShadow="False" Margin="0,10,0,0">
                <HorizontalStackLayout Spacing="15">
                    <Label Text="🔒" FontSize="20" VerticalOptions="Center"/>
                    <VerticalStackLayout Spacing="2">
                        <Label Text="Пароль" FontSize="12" TextColor="Gray"/>
                        <Entry x:Name="Pass"
                               Placeholder="Введите пароль" 
                               IsPassword="True"
                               ClearButtonVisibility="WhileEditing"
                               TextColor="Black"/>
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Frame>

            <!--Подтверждение пароля-->
            <Frame BackgroundColor="#F8F9FA" CornerRadius="10" HasShadow="False" Margin="0,10,0,0">
                <HorizontalStackLayout Spacing="15">
                    <Label Text="🔒" FontSize="20" VerticalOptions="Center"/>
                    <VerticalStackLayout Spacing="2">
                        <Label Text="Пароль" FontSize="12" TextColor="Gray"/>
                        <Entry x:Name="Confirm"
                               Placeholder="Подтвердите пароль" 
                               IsPassword="True"
                               ClearButtonVisibility="WhileEditing"
                               TextColor="Black"/>
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </Frame>

            <!-- Кнопка сброс пароля -->
            <Button
                BackgroundColor="White"
                Text ="Сменить пароль"
                FontSize="25"
                TextColor="Black"
                Margin="0, 20"
                CornerRadius="16"
                WidthRequest="350"
                HorizontalOptions="Center"
                Clicked="Pass_Res"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
